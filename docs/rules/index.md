# Rule catalog

Generated by `skillscan rules export --format md`. **54 rules** across 8 categories.

> This page is a snapshot. Run the command above to regenerate after adding rules.

## Injection (9)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-INJ-001` | critical | Zero-width character in prompt content | Strip zero-width unicode characters from prompt content. They are invisible to humans but parsed by the model, and are a… |
| `SKILL-INJ-003` | high | Prompt-injection trigger phrase | Quote or paraphrase the phrase. If you must document prompt-injection attacks, place the phrase inside a fenced code blo… |
| `SKILL-INJ-004` | high | Role-switch marker in content | Remove special role markers from skill content. Tokens like `<\|system\|>`, `[INST]`, or markdown headings such as `### … |
| `SKILL-INJ-005` | medium | Long base64-like blob in markdown content | Decode and inspect the blob. If it must remain in the skill, store it in a separate non-markdown file and reference it e… |
| `SKILL-INJ-007` | high | Instruction-shaped phrase hidden in HTML comment | Markdown rendering hides HTML comments from humans, but agents may still parse them. Move legitimate instructions to pla… |
| `SKILL-INJ-008` | medium | Suspiciously long single line in markdown content | Long single lines often hide an encoded payload or a stretched-out prompt-injection string. Break the content onto multi… |
| `SKILL-INJ-009` | critical | Invisible unicode tag character in prompt content | Unicode Tag characters (U+E0020 – U+E007F) are an invisible alphabet that LLMs can read but humans cannot. They are an… |
| `SKILL-INJ-010` | critical | Unicode bidirectional override character | Bidirectional control codepoints (U+202A – U+202E, U+2066 – U+2069) reverse the visual order of characters. They are… |
| `SKILL-INJ-011` | high | Tool-use markup in skill content | Tags like `<tool_use>`, `<tool_result>`, or `<function_calls>` are parsed by the agent runtime as live tool-call markup.… |

## Permissions (6)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-PRM-001` | high | Unscoped Bash permission | Restrict `allowed-tools` to specific commands, e.g. `Bash(git status)` or `Bash(npm test)`. |
| `SKILL-PRM-002` | high | Write/Edit permission targets a sensitive path | Narrow the path scope of Write/Edit/MultiEdit. System directories and credential dotfiles should never be writable by a … |
| `SKILL-PRM-003` | high | Read permission targets sensitive dotfiles | Read scope should not include credential dotfiles. If you genuinely need a value from one, accept it via an environment … |
| `SKILL-PRM-004` | medium | Unscoped WebFetch or WebSearch permission | Restrict WebFetch/WebSearch to a host allowlist, e.g. `WebFetch(docs.example.com)`. Unscoped network access is a common … |
| `SKILL-PRM-006` | medium | allowed-tools not declared | Declare an explicit `allowed-tools:` list. An empty or missing list grants whatever the host environment provides — of… |
| `SKILL-PRM-007` | medium | Excessive allowed-tools count | Skill grants more than 15 tools. Pare down to the minimum required — every additional tool widens the blast radius of … |

## Exfiltration (8)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-EXF-001` | high | Outbound URL to a paste/file-share service | URLs pointing at pastebin-style services or anonymous file hosts are classic exfiltration sinks. Move the value to a run… |
| `SKILL-EXF-002` | medium | Suspiciously long subdomain in URL or DNS query | Long subdomain labels are a known DNS-exfiltration channel: the attacker encodes data into a hostname under a domain the… |
| `SKILL-EXF-003` | high | Skill script reads credential directories | `~/.ssh`, `~/.aws`, `~/.kube` and friends hold long-lived credentials. A skill script should not read them — accept th… |
| `SKILL-EXF-004` | high | Environment variables piped to a network tool | `env`, `printenv`, or `set` piped to curl/wget/nc dumps every environment variable — including credentials — to an a… |
| `SKILL-EXF-005` | medium | Clipboard access in skill script | Clipboard reads from a skill script can exfiltrate copied secrets silently. Justify with an inline comment if intentiona… |
| `SKILL-EXF-006` | high | Outbound webhook URL | Webhooks are a common exfiltration channel. Remove the URL, or move it to a runtime-supplied environment variable so the… |
| `SKILL-EXF-007` | high | Outbound netcat / ncat connection | `nc`/`ncat` opens a raw TCP/UDP connection — a classic exfiltration and reverse-shell channel. If you need outbound ne… |
| `SKILL-EXF-008` | high | Cloud instance-metadata endpoint reference | The IMDS endpoints (`169.254.169.254` on AWS/GCP/Azure, `metadata.google.internal`) return short-lived credentials and i… |

## Supply chain (6)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-SUP-001` | high | curl/wget piped into a shell | Download the install script, verify a checksum or signature, then execute it. Piping curl/wget directly into a shell run… |
| `SKILL-SUP-002` | medium | Unpinned `pip install` | Pin the dependency version with `==<version>`, or install from a lockfile (`pip install -r requirements.txt`). Unpinned … |
| `SKILL-SUP-003` | high | Fetched-then-executed binary | Downloading a file and making it executable in the same script is the canonical "drop a binary and run it" pattern. Vend… |
| `SKILL-SUP-004` | critical | Imports or installs a known PyPI typosquat | This is a known typosquat of a legitimate PyPI package. Almost certainly malicious. Switch to the canonical name and aud… |
| `SKILL-SUP-005` | medium | Runtime git clone of remote repository | Cloning a remote repo at runtime introduces a moving supply-chain target. Vendor the dependency, or pin to a specific co… |
| `SKILL-SUP-006` | medium | Global npm install at runtime | `npm install -g` writes a package to the user's global node_modules, altering tooling outside the skill's directory and … |

## Obfuscation (5)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-OBF-001` | high | Dynamic code execution (eval/exec) | `eval` and `exec` of a string allow arbitrary code execution and are a common obfuscation vehicle. Refactor to direct ca… |
| `SKILL-OBF-002` | medium | Long hex blob in content | Long hex strings are a common encoding for hidden payloads. Decode and inspect. If the value is legitimate binary data (… |
| `SKILL-OBF-003` | high | Unsafe Python deserialization (pickle/marshal) | `pickle.loads`, `marshal.loads`, `dill.loads` and `joblib.load` will execute arbitrary code embedded in the input. Switc… |
| `SKILL-OBF-004` | medium | Python compile() of dynamic source | `compile()` of a non-literal first argument is a step toward dynamic code execution and a common obfuscation vehicle. Re… |
| `SKILL-OBF-005` | medium | Python decompress of opaque payload | `zlib.decompress`, `gzip.decompress`, `bz2.decompress`, and `lzma.decompress` followed by `exec`/`eval` is the canonical… |

## Secrets (9)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-SEC-001` | critical | Hardcoded secret in skill bundle | Remove the secret from the bundle, rotate it immediately, and load credentials from environment variables or a secret st… |
| `SKILL-SEC-002` | critical | Google API key in bundle content | Google API keys (prefix `AIza`) provide quota-billed access to Google Cloud services. Rotate the key in the Google Cloud… |
| `SKILL-SEC-003` | critical | Slack token in bundle content | Slack tokens grant access to channel history and outbound posting. Revoke the token in Slack admin and remove it from th… |
| `SKILL-SEC-004` | critical | Stripe live API key in bundle content | Stripe live keys allow real money movement. Rotate in the Stripe dashboard, scan logs for unauthorized use, and remove t… |
| `SKILL-SEC-005` | low | JWT-shaped token in bundle content | JWTs often carry session or service tokens. Confirm whether the value is an example or a real credential, and remove if … |
| `SKILL-SEC-006` | critical | Twilio account SID or API key | Twilio account SIDs (AC...) and API keys (SK...) authorize sending SMS and voice calls billed to the account. Rotate in … |
| `SKILL-SEC-007` | critical | SendGrid API key in bundle content | SendGrid API keys can send mail under your account's reputation. Rotate via the SendGrid dashboard, audit recent mail se… |
| `SKILL-SEC-008` | critical | Anthropic API key in bundle content | Anthropic API keys grant access to the model API and bill against the account. Rotate via the Anthropic Console, audit r… |
| `SKILL-SEC-009` | critical | OpenAI API key in bundle content | OpenAI keys (legacy `sk-…48-chars` or modern `sk-proj-…` / `sk-svcacct-…`) grant access to the model API and bill … |

## Compliance (5)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-CMP-001` | low | Missing or too-short description | Add a `description:` field of at least 20 characters explaining what the skill does. |
| `SKILL-CMP-002` | low | Missing version field | Add a `version:` field to the frontmatter so consumers can pin to a known revision. |
| `SKILL-CMP-003` | low | Missing license declaration | Declare a `license:` in the frontmatter, or ship a `LICENSE`/`COPYING` file at the skill root. |
| `SKILL-CMP-004` | low | Oversized skill bundle | Skills above 10 MiB are unusual. If the bundle contains large model artifacts or data files, host them externally and re… |
| `SKILL-CMP-006` | low | SKILL.md is suspiciously large | A SKILL.md over 100 KiB is unusual; long single instruction files are harder to audit and tend to bury intent. Split int… |

## Code quality (6)

| ID | Severity | Name | Remediation |
|----|----------|------|-------------|
| `SKILL-CQ-001` | high | subprocess call with shell=True | Pass the command as a list (`subprocess.run(["git", "status"])`) without `shell=True`. With `shell=True`, any unsanitize… |
| `SKILL-CQ-002` | high | os.system call | `os.system` runs its argument through a shell with no escaping. Switch to `subprocess.run([...])` with the command as a … |
| `SKILL-CQ-003` | high | eval/exec call (AST-precise) | `eval` and `exec` execute arbitrary code. With a non-literal argument they are a direct RCE channel. Refactor to direct … |
| `SKILL-CQ-004` | high | Unsafe Python deserialization (AST-precise) | `pickle.loads`, `marshal.loads`, `dill.loads`, `joblib.load` execute arbitrary code embedded in their input. Switch to a… |
| `SKILL-CQ-005` | high | bash `eval` (AST-precise) | `eval` in bash runs its argument through the parser a second time, executing whatever the argument expands to. If any in… |
| `SKILL-CQ-006` | high | bash `source`/`.` with a variable target | Sourcing a file whose path is held in a variable is equivalent to `eval`-ing whatever the attacker can write to that pat… |

